// Create an array of fruits and use forEach to print each fruit
const fruits = ['banana', 'mango', 'lanzones', 'rambutan', 'dragonfruit'];
fruits.forEach((fruit) => 
{
    console.log(fruit * 2);
});

